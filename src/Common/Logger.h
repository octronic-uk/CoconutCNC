#pragma once

/**
  Include SPDLOG headers
*/

#include <spdlog/spdlog.h>
#include <spdlog/sinks/stdout_color_sinks.h>

using spdlog::info;
using spdlog::debug;
using spdlog::warn;
using spdlog::error;
