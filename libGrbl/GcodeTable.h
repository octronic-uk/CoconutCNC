// This file is a part of "CoconutCNC" application.
// Copyright 2015-2016 Hayrullin Denis Ravilevich

#pragma once

#include "GCodeCommand.h"

namespace Coconut
{
    /*
	class GCodeTable
	{
	public:
		GCodeTable();
		~GCodeTable();
		GcodeCommand* Data(int index) const;
		bool SetData(int index, GcodeCommand* value);
		bool insertRow(int row, GcodeCommand* value);
		bool removeRow(int row);
		bool removeRows(int row, int count);
		int rowCount() const;
		int columnCount() const;
		vector<string> headerData() const;
		vector<GcodeCommand*>& Data();
		void SetCommandData(vector<GcodeCommand*> items);
		void Clear();

	private:
		vector<GcodeCommand*> mData;
		vector<string> mHeaders;
	};
    */

}
